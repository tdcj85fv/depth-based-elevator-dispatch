# depth-based-elevator-dispatch
This project implements a depth-only, privacy-preserving elevator demand estimation system using LiDAR-style depth data. Instead of counting people or tracking movement, the system computes waiting-area density to optimize elevator dispatch and positioning.
